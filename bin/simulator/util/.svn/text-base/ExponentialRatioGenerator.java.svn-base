package simulator.util;

public class ExponentialRatioGenerator 
{
	public static double generate(double randomNumber, double taxa)
	{
		return 0 - Math.log(randomNumber)/(Math.log(Math.E)*taxa);
	}
}